[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "zneondrive-reference"
version = "0.2.0"
description = "Executable server-authoritative domain contracts for PROJECT: NEON DRIVE"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
